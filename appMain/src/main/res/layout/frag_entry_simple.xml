<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.cartlc.tracker.viewmodel.EntrySimpleViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="@{viewModel.showing ? View.VISIBLE : View.GONE}">

        <EditText
            android:id="@+id/entry_simple_edit_text"
            style="@style/EntryText"
            android:layout_width="@dimen/setup_frame_width"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:inputType="textCapWords"
            android:maxLines="1"
            android:text="@{viewModel.simpleText}"
            android:hint="@{viewModel.simpleHint}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/entry_simple_help_text"
            style="@style/HintText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:text="@{viewModel.helpText}"
            android:visibility="@{viewModel.helpText != null ? View.VISIBLE : View.GONE}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/entry_simple_edit_text"
            tools:text="Hint" />

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>
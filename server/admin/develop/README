Login To Server: 
------------
ssh -i ~/.ssh/cartlc.pem ubuntu@cartlc.arqnetworks.com


# ssh -i ~/.ssh/FleetTLC.pem ubuntu@ec2-18-219-26-13.us-east-2.compute.amazonaws.com
ssh -i ~/.ssh/FleetTLC.pem ubuntu@ec2-18-216-214-167.us-east-2.compute.amazonaws.com
ssh -i ~/.ssh/FleetTLC.pem ubuntu@fleetdev.arqnetworks.com

fleetdev.arqnetworks.com/phpmyadmin
cartlc/cartlc
root/cartlc

Web Access:
------------
http://fleetdev.arqnetworks.com/

Starting Play:
------------
cd play
sbt -mem 2048
run

Deployment:
----------
Inc version in server/play/build.sbt
sbt -mem 2048
dist
r-wrap-server -d
follow instructions

Amazon:
-------
https://aws.amazon.com/

PHPMyAdmin:
   http://52.14.73.217/phpmyadmin OR
   http://cartlc.arqnetworks.com/phpmyadmin
   User: root p/w: cartlc
   Additional: cartlc p/w: cartcl#@!

Copy files from local machine to server
cp credentials ~/.aws
cd ~/fleet/s3/fleettlc
aws s3 sync . s3://cartlc

Database:
--------
mysqldump -u cartlc -p cartlc > dump-`date +%Y-%m-%d`.sql
p/w: cartcl

scp -i ~/.ssh/FleetTLC.pem somefile ubuntu@fleetdev.arqnetworks.com:

Restore:
mysql -u cartlc -p
mysql> use cartlc
mysql> source dump.sql
